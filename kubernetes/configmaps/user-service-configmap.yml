apiVersion: v1
kind: ConfigMap
metadata:
  name: user-service-config
  namespace: doctor-platform
data:
  database.url: "jdbc:postgresql://postgres-users:5432/doctor_users"
  spring.profiles.active: "kubernetes"
  logging.level: "INFO"
